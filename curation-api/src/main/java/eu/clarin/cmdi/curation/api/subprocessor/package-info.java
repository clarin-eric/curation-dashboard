/**
 * This package and it's sub-packages groups the entity specific sub-processors: a processor creates a chain of
 * sub-processors. In most cases a sub-processor creates one section report (f.e. the sub-processor /instance/FileSizeValidator
 * creates the section report /instance/sec/FileReport)
 *
 * @author Wolfgang Walter SAUER (wowasa) &lt;clarin@wowasa.com&gt;
 */

package eu.clarin.cmdi.curation.api.subprocessor;
